/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DimensionedCroppedImageComponent } from './dimensioned-cropped-image.component';

describe('Component: DimensionedCroppedImage', () => {
  it('should create an instance', () => {
    let component = new DimensionedCroppedImageComponent();
    expect(component).toBeTruthy();
  });
});
